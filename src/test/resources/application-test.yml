spring:
  config:
    import: ""
  cloud:
    vault:
      uri: ${VAULT_URI:http://localhost:8200}
      authentication: TOKEN
      token: ${VAULT_TOKEN:root}
      ssl:
        enabled: false
      kv:
        enabled: true
        backend: secret
        default-context: myapp
    bootstrap:
      enabled: false